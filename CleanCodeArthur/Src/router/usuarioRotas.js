const { Router } = require('express');

const router = Router();
//Criar
router.post('/',);
//Atualizar
router.put('/:id',);
//Excluir
router.delete('/:id',);
//Buscar único
router.get('/:id',);
//Buscar todos
router.get('/',);

module.exports = router;